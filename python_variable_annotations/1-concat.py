#!/bin/env python3
"""Script to concatenate two strings"""


def concat(str1: str, str2: str) -> str:
    """Function to concatenate two strings and return the result as a string"""
    return str1 + str2
